apiVersion: v1
kind: Service
metadata:
  name: {{ include "topology-builder.fullname" . }}
  labels:
    {{- include "topology-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "topology-builder.selectorLabels" . | nindent 4 }}
